#include <string>
#include <vector>

#include <iostream>
#include <unordered_map>

using namespace std;

vector<int> solution(vector<string> genres, vector<int> plays) {

	vector<int> answer;
	unordered_map<int, vector<int, string>> map;

	for (int i = 0; i < genres.size(); i++)
	{
		
		map.emplace(i, )
	}

	return answer;
}


int main()
{
	std::cout << "Hello World!\n";
}

